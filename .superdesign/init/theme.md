# Theme

## Compact token summary

- Colors: browser defaults; no application palette exists yet.
- Typography: browser default sans-serif; no font files or type scale exists yet.
- Spacing: browser defaults; no spacing tokens exist yet.
- Radius and shadows: none defined.
- Breakpoints: none defined.
- CSS approach: unstyled semantic HTML; no Tailwind, CSS Modules, component library, or global stylesheet.

## Raw source dumps

No `globals.css`, CSS variables, Tailwind configuration, theme provider, or design-token file exists in the current repository. The root layout source is preserved in `layouts.md`; `next.config.ts` contains build configuration only and defines no visual tokens.
